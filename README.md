
  # SkillUp

  This is a code bundle for SkillUp. The original project is available at https://www.figma.com/design/wpLsKmiQRJHG2XS5UsuPk3/SkillUp.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  